# Build-only dependencies — NOT installed by setup.sh or launch.sh.
# Used only by the Windows frozen build in
# .github/workflows/build-release.yml (pyinstaller FableGear_win.spec).
# macOS ships the thin bootstrap bundle instead (release-zip.yml), so no
# PyInstaller step runs for macOS.
#
# Usage: pip install -r requirements_build.txt

pyinstaller>=6.0
# Bundles Python + all deps into a standalone Windows executable.
