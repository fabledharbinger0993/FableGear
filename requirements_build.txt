# Build-only dependencies — NOT installed by setup.sh or launch.sh.
# Used for a manual/local Windows frozen build (pyinstaller FableGear_win.spec).
# Not currently wired into any GitHub Actions workflow — the only workflow
# that exists, .github/workflows/build-release.yml, builds and publishes the
# macOS thin bootstrap bundle only (via build_release.sh, no PyInstaller step
# for either platform runs in CI today).
#
# Usage: pip install -r requirements_build.txt

pyinstaller>=6.0
# Bundles Python + all deps into a standalone Windows executable.
